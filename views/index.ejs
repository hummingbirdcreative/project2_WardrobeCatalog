<nav>
    <a href="/items/new">Create New Item</a>
</nav>
<ul>
    
    <% allItems.forEach(item => { %>
        <li>
            <a href="/items/<%=item._id%>"><%= item.img %></a>
        </li>
    <% }) %>
